{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in *.rs) cargo fmt --manifest-path \"$CLAUDE_PROJECT_DIR/core/Cargo.toml\" >/dev/null 2>&1 || true;; esac",
            "statusMessage": "cargo fmt"
          }
        ]
      }
    ]
  }
}
